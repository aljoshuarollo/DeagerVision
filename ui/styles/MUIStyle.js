import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#2397a8',
            contrastText: '#fff',
        },
        neutral: {
            main: '#64748B',
            contrastText: '#fff',
        },
        cancel: {
            main: '#e53e3e',
            contrastText: '#fff',
        },
    },
});
